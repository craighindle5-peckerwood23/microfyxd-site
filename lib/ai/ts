const res = await fetch("/api/ai", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    messages: [
      { role: "system", content: "You are a claims reasoning engine." },
      { role: "user", content: "Score this claim and explain why: ..." },
    ],
  }),
});

const data = await res.json();
console.log(data.output);
